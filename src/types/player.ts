export type Player = {
  avatar_url: string;
  avg_gross_score: number;
  avg_net_score: number;
  best_gross_score: number;
  best_net_score: number;
  gross_points: number;
  name: string;
  net_points: number;
  player_id: string;
};
