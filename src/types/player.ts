export type Player = {
  avatar_url: string;
  avg_gross: number;
  avg_net: number;
  best_gross: number;
  best_net: number;
  gross_points: number;
  name: string;
  net_points: number;
  net_wins: number;
  gross_wins: number;
  player_id: string;
  player_color: string;
  invite_email: string;
  player_role: string;
  user_id: string;
};
